---
import BaseLayout from "@/layouts/BaseLayout.astro";
import MapSection from "@/components/main/mapSection/MapSection.astro";
import CtaSection from "@/components/main/CTA/CtaSection.astro";
import { useTranslations } from "@/i18n/utils";

const t = useTranslations(Astro.currentLocale);
---

<BaseLayout siteTitle={t("page.about.siteTitle")} ogTitle={t("page.about.ogTitle")} siteDescription={t("page.about.siteDescription")} ogDescription={t("page.about.ogDescription")}>
  <slot />
  <MapSection />
  <CtaSection />
</BaseLayout>
