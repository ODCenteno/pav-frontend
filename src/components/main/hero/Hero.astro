---
import "./hero.css";
import { useTranslations } from "../../../i18n/utils";
import { navigation } from "@/utils/navigation";
import type { HeroData } from "@/types/homepage.type";

interface Props {
  data: HeroData;
}

const { data } = Astro.props;
const locale = Astro.currentLocale || "es";
const t = useTranslations(locale);
---

<section class="hero section-padding">
  <div class="container hero-container">
    <div class="hero-content">
      <h1 data-key="hero_title">
        {data.title}{" "}
        {data.titleHighlight && <span class="highlight-text">{data.titleHighlight}</span>}
      </h1>
      <p data-key="hero_desc">{data.description}</p>
      <div class="hero-actions">
        <a class="btn btn-accent" href={data.ctaLink || navigation.sites(locale)} data-key="hero_cta">{data.ctaLabel || t("hero.cta")}</a>
      </div>
    </div>
    <div class="hero-images">
      {data.images.map((img, i) => (
        <div class={`hero-img-card img-${i + 1}`}>
          <img src={img.url} alt={img.alt} loading="eager" />
        </div>
      ))}
    </div>
  </div>
  <div class="hero-wave" aria-hidden="true">
    <svg viewBox="0 0 1440 40" preserveAspectRatio="none" fill="none">
      <path d="M0,25 C240,45 480,5 720,25 C960,45 1200,5 1440,25 L1440,40 L0,40 Z" fill="var(--color-bone, #FDFCF8)"></path>
    </svg>
  </div>
</section>